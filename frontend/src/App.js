import { useEffect, useState } from "react";
import axios from "axios";
import { Routes, Route } from "react-router-dom";
import { BookOpen, Compass, GraduationCap, Home, ImagePlus, LogIn, MessageSquare, Plus, Search, Shield, UserRound, Users, X } from "lucide-react";
import "@/App.css";

const api = axios.create({ baseURL: `${process.env.REACT_APP_BACKEND_URL}/api`, withCredentials: true });
const founderData = [{ name: "oildrila", email: "founder-email@edit-me.com", linkedin: "linkedin.com/in/oildrila" }, { name: "Arjun Jha", email: "arjunjha056@edit-me.com", linkedin: "linkedin.com/in/arjunjha056" }];
const fallbackRooms = [{id:"academics",title:"Academics & Research",description:"Ideas, papers, labs, and learning resources",members:128},{id:"placements",title:"Placements & Careers",description:"Internships, interviews, and opportunities",members:96},{id:"campus",title:"Campus Life",description:"Clubs, hostel life, events, and everyday NSEC",members:74}];
const formatError = e => { const d=e.response?.data?.detail; return Array.isArray(d)?d.map(x=>x.msg).join(" "):d || "Something went wrong. Try again."; };
const compressImage = file => new Promise((resolve,reject)=>{ const reader=new FileReader(); reader.onload=()=>{const img=new Image();img.onload=()=>{const scale=Math.min(1,800/img.width),canvas=document.createElement("canvas");canvas.width=img.width*scale;canvas.height=img.height*scale;canvas.getContext("2d").drawImage(img,0,0,canvas.width,canvas.height);resolve(canvas.toDataURL("image/jpeg",.6));};img.onerror=reject;img.src=reader.result;};reader.readAsDataURL(file); });

function Auth({onAuth}) { const [mode,setMode]=useState("login"),[form,setForm]=useState({name:"",email:"",password:"",role:"student"}),[error,setError]=useState(""); const submit=async e=>{e.preventDefault();try{onAuth((await api.post(`/auth/${mode}`,form)).data)}catch(err){setError(formatError(err))}};return <main className="auth-page"><section className="auth-visual"><div className="brand-mark"><span>n</span> NSEC / ACADEMIA</div><div><p className="eyebrow">NETAJI SUBHAS ENGINEERING COLLEGE</p><h1>Where campus<br/><em>thinking</em> connects.</h1><p className="hero-note">A private academic network for ideas, people, and the work happening at NSEC.</p></div><div className="campus-strip"><img src="https://images.unsplash.com/photo-1591123120675-6f7f1aae0e5b?crop=entropy&cs=srgb&fm=jpg&q=85" alt="Campus"/><span>Explore the people behind the progress →</span></div></section><section className="auth-panel"><div className="auth-box"><p className="eyebrow">{mode==="login"?"WELCOME BACK":"JOIN THE NETWORK"}</p><h2>{mode==="login"?"Sign in to your campus.":"Create your campus profile."}</h2><p className="muted">Connect with classmates, teachers, and ideas.</p>{error&&<div className="error" data-testid="auth-error">{error}</div>}<form onSubmit={submit} data-testid="auth-form">{mode==="register"&&<input data-testid="register-name-input" placeholder="Full name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/>}<input data-testid="auth-email-input" type="email" placeholder="College email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/><input data-testid="auth-password-input" type="password" placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/>{mode==="register"&&<select data-testid="role-select" value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option value="student">Student</option><option value="teacher">Teacher</option><option value="founder">Founder / Admin</option></select>}<button className="primary-btn" data-testid="auth-submit-button">{mode==="login"?"Enter NSEC":"Create profile"}<LogIn size={17}/></button></form><button className="google-btn" data-testid="google-login-button" onClick={()=>setError("Google login is ready for OAuth configuration by an administrator.")}>G <span>Continue with Google</span></button><button className="text-btn" data-testid="auth-mode-toggle" onClick={()=>setMode(mode==="login"?"register":"login")}>{mode==="login"?"New to NSEC? Create an account":"Already have an account? Sign in"}</button><button className="access-link" data-testid="access-issue-button" onClick={()=>setError("For access issues, contact the NSEC founders from the Info section.")}>Having trouble accessing your account?</button></div></section></main> }

function TeacherComposer({onSaved}) { const [topic,setTopic]=useState(""),[details,setDetails]=useState("");const submit=async e=>{e.preventDefault();await api.post("/teacher-updates",{topic,details});setTopic("");setDetails("");onSaved()};return <form className="teacher-form" onSubmit={submit} data-testid="teacher-update-form"><input data-testid="teacher-topic-input" placeholder="Today’s topic" value={topic} onChange={e=>setTopic(e.target.value)} required/><textarea data-testid="teacher-details-input" placeholder="What did you teach today?" value={details} onChange={e=>setDetails(e.target.value)} required/><button className="primary-btn compact" data-testid="teacher-update-submit-button">Publish teaching note <Plus size={16}/></button></form> }
function Section({title,eyebrow,intro,children}) { return <><div className="page-head section-head"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="muted intro">{intro}</p></div></div>{children}</>; }

function RoomView({rooms}) { const [selected,setSelected]=useState(rooms[0]?.id),[posts,setPosts]=useState([]),[body,setBody]=useState(""),[replyTo,setReplyTo]=useState(null);useEffect(()=>{if(selected)api.get(`/rooms/${selected}/posts`).then(r=>setPosts(r.data)).catch(()=>{})},[selected]);const publish=async e=>{e.preventDefault();if(!body.trim())return;await api.post(`/rooms/${selected}/posts`,{body,parent_id:replyTo});setBody("");setReplyTo(null);const r=await api.get(`/rooms/${selected}/posts`);setPosts(r.data)};const react=async id=>{const r=await api.post(`/rooms/${selected}/posts/${id}/react`);setPosts(posts.map(p=>p.id===id?r.data:p))};return <Section title="Discussion rooms" eyebrow="OPEN CONVERSATIONS" intro="Choose a room, share a thought, and build on the ideas already moving through NSEC."><div className="room-layout"><div className="room-list">{rooms.map(r=><button key={r.id} className={selected===r.id?"room-select active":"room-select"} data-testid={`room-select-${r.id}`} onClick={()=>setSelected(r.id)}><strong>{r.title}</strong><small>{r.members} members</small></button>)}</div><div className="conversation"><div className="conversation-head"><MessageSquare size={19}/><strong>{rooms.find(r=>r.id===selected)?.title}</strong></div><div className="conversation-posts">{posts.length?posts.map(p=><article className={p.parent_id?"room-post reply":"room-post"} key={p.id} data-testid="room-post"><div className="post-top"><div className="avatar small">{p.author[0]}</div><div><strong>{p.author}</strong><small>{p.role}</small></div></div><p>{p.body}</p><div className="room-actions"><button data-testid={`room-react-${p.id}`} onClick={()=>react(p.id)}>♡ {p.reactions?.thoughtful||0}</button><button data-testid={`room-reply-${p.id}`} onClick={()=>setReplyTo(p.id)}>Reply</button></div></article>):<div className="empty">Start this room’s first conversation.</div>}</div><form className="room-composer" onSubmit={publish} data-testid="room-composer">{replyTo&&<div className="replying">Replying to a discussion <button type="button" data-testid="cancel-reply-button" onClick={()=>setReplyTo(null)}><X size={14}/></button></div>}<textarea data-testid="room-post-input" placeholder="Add to the conversation..." value={body} onChange={e=>setBody(e.target.value)} required/><button className="primary-btn compact" data-testid="room-post-submit-button">Post to room <Plus size={16}/></button></form></div></div></Section> }

function ProfileView({user,onSaved}) { const [form,setForm]=useState({...user,interests:(user.interests||[]).join(", ")}),[message,setMessage]=useState("");const file=async e=>setForm({...form,avatar:await compressImage(e.target.files[0])});const submit=async e=>{e.preventDefault();const payload={...form,interests:form.interests.split(",").map(x=>x.trim()).filter(Boolean)};const r=await api.put("/profile",payload);onSaved(r.data);setMessage("Profile saved")};return <Section title="Your academic profile" eyebrow="PROFILE / EDIT" intro="Make it easier for the right people to find your work and interests."><form className="profile-form" onSubmit={submit} data-testid="profile-form"><div className="profile-edit-top"><div className="avatar profile-avatar">{form.avatar?<img src={form.avatar} alt="Profile preview"/>:form.name?.[0]}</div><label className="attach" data-testid="profile-image-label"><ImagePlus size={17}/> Change photo<input data-testid="profile-image-input" type="file" accept="image/*" onChange={file}/></label></div><div className="profile-fields"><label>Full name<input data-testid="profile-name-input" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Headline<input data-testid="profile-headline-input" placeholder="e.g. Computer science student" value={form.headline} onChange={e=>setForm({...form,headline:e.target.value})}/></label><label>Department<input data-testid="profile-department-input" placeholder="e.g. Information Technology" value={form.department} onChange={e=>setForm({...form,department:e.target.value})}/></label><label>Relationship status<select data-testid="profile-relationship-input" value={form.relationship} onChange={e=>setForm({...form,relationship:e.target.value})}><option>Single</option><option>In a relationship</option><option>Prefer not to say</option></select></label><label>Academic interests<input data-testid="profile-interests-input" placeholder="AI, robotics, design" value={form.interests} onChange={e=>setForm({...form,interests:e.target.value})}/></label><label>Biography<textarea data-testid="profile-bio-input" value={form.bio} onChange={e=>setForm({...form,bio:e.target.value})}/></label></div>{message&&<div className="saved" data-testid="profile-saved-message">{message}</div>}<button className="primary-btn" data-testid="profile-save-button">Save profile</button></form></Section> }

function Discovery() { const [q,setQ]=useState(""),[data,setData]=useState({people:[],rooms:[],updates:[]});const search=async e=>{e.preventDefault();setData((await api.get("/discover",{params:{q}})).data)};return <Section title="Find your campus" eyebrow="DISCOVERY / NSEC" intro="Search people, discussion rooms, and teaching notes across the community."><form className="discover-search" onSubmit={search}><Search size={18}/><input data-testid="discovery-search-input" placeholder="Search people, departments, rooms..." value={q} onChange={e=>setQ(e.target.value)}/><button className="primary-btn compact" data-testid="discovery-search-button">Search</button></form><div className="discover-grid"><div><h3>People</h3>{data.people.map(p=><article className="result" key={p.id} data-testid="person-result"><div className="avatar small">{p.name[0]}</div><div><strong>{p.name}</strong><small>{p.role} · {p.department||"NSEC community"}</small><p>{p.headline||p.bio||"Open to campus conversations"}</p></div></article>)}</div><div><h3>Rooms & notes</h3>{[...data.rooms.map(r=>({...r,type:"Room"})),...data.updates.map(u=>({...u,title:u.topic,type:"Teacher note"}))].map((x,i)=><article className="result compact-result" key={x.id||i} data-testid="discovery-result"><div><small>{x.type}</small><strong>{x.title}</strong><p>{x.description||x.details}</p></div></article>)}</div></div></Section> }

function Shell({user,onLogout}) { const [active,setActive]=useState("feed"),[posts,setPosts]=useState([]),[rooms,setRooms]=useState(fallbackRooms),[updates,setUpdates]=useState([]),[body,setBody]=useState(""),[image,setImage]=useState("");const refresh=()=>{api.get("/feed").then(r=>setPosts(r.data));api.get("/rooms").then(r=>setRooms(r.data));api.get("/teacher-updates").then(r=>setUpdates(r.data))};useEffect(refresh,[]);const publish=async e=>{e.preventDefault();if(!body.trim())return;await api.post("/feed",{body,image});setBody("");setImage("");refresh()};const file=async e=>setImage(await compressImage(e.target.files[0]));const nav=[["feed","Feed",Home],["rooms","Discussion rooms",MessageSquare],["teachers","Teacher updates",GraduationCap],["discover","Discover",Compass],["profile","My profile",UserRound],["info","Info & founders",Users]];return <div className="app-shell"><header className="topbar"><div className="brand-mark"><span>n</span> NSEC / ACADEMIA</div><div className="top-actions"><span className="role-chip"><span className="online-dot"/> {user.role}</span><button data-testid="logout-button" className="icon-btn" onClick={onLogout}><X size={18}/></button></div></header><div className="layout"><aside className="sidebar"><div className="profile-mini"><div className="avatar">{user.name[0]}</div><strong data-testid="current-user-name">{user.name}</strong><small>{user.email}</small><span>{user.relationship}</span></div><nav>{nav.map(([id,label,Icon])=><button key={id} data-testid={`nav-${id}-button`} className={active===id?"nav-item active":"nav-item"} onClick={()=>setActive(id)}><Icon size={18}/>{label}</button>)}</nav><div className="sidebar-foot"><Shield size={16}/><span>Private NSEC community</span></div></aside><main className="content">{active==="feed"&&<><div className="page-head"><div><p className="eyebrow">NSEC COMMUNITY / TODAY</p><h1>Good morning, {user.name.split(" ")[0]}.</h1></div></div><form className="composer" onSubmit={publish} data-testid="post-composer"><div className="avatar small">{user.name[0]}</div><textarea data-testid="post-body-input" placeholder="Share an idea, question, or small win..." value={body} onChange={e=>setBody(e.target.value)}/>{image&&<img className="image-preview" src={image} alt="Selected preview"/>}<div className="composer-actions"><label className="attach" data-testid="image-upload-label"><ImagePlus size={17}/> Add image<input data-testid="image-upload-input" type="file" accept="image/*" onChange={file}/></label><button className="primary-btn compact" data-testid="publish-post-button">Publish <Plus size={16}/></button></div></form><div className="feed-list">{posts.map(p=><article className="post" key={p.id} data-testid="feed-post"><div className="post-top"><div className="avatar small">{p.author[0]}</div><div><strong>{p.author}</strong><small>{p.role} · NSEC community</small></div></div><p>{p.body}</p>{p.image&&<img className="post-image" src={p.image} alt="Post attachment"/>}</article>)}</div></>}{active==="rooms"&&<RoomView rooms={rooms}/>} {active==="teachers"&&<Section title="Teacher updates" eyebrow="WHAT WAS TAUGHT TODAY" intro="Daily notes from the people guiding the next generation of builders.">{(user.role==="teacher"||user.role==="founder")&&<TeacherComposer onSaved={refresh}/>}<div className="updates">{updates.map(u=><article className="update" key={u.id} data-testid="teacher-update"><div className="date-tag">TODAY</div><div><strong>{u.topic}</strong><p>{u.details}</p><small>Shared by {u.teacher}</small></div></article>)}</div></Section>}{active==="discover"&&<Discovery/>}{active==="profile"&&<ProfileView user={user} onSaved={u=>{user=Object.assign(user,u);setActive("profile")}}/>}{active==="info"&&<Section title="The people behind NSEC Academia" eyebrow="INFO / ACCESS SUPPORT" intro="A shared space for the NSEC community, shaped by its founders."><div className="founder-grid">{founderData.map(f=><article className="founder" key={f.name} data-testid="founder-card"><div className="founder-photo">{f.name[0]}</div><div><p className="eyebrow">FOUNDER</p><h3>{f.name}</h3><p className="muted">{f.email}</p><p className="linkedin">in&nbsp; {f.linkedin}</p></div></article>)}</div><div className="access-note"><BookOpen size={22}/><div><strong>Need help getting access?</strong><p>Use your college email when registering. For account issues, reach out to a founder.</p></div></div></Section>}</main></div></div> }

function App(){const [user,setUser]=useState(null),[checking,setChecking]=useState(true);useEffect(()=>{api.get("/auth/me").then(r=>setUser(r.data)).catch(()=>{}).finally(()=>setChecking(false))},[]);if(checking)return <div className="loading" data-testid="loading-state">Loading NSEC Academia…</div>;return user?<Shell user={user} onLogout={async()=>{await api.post("/auth/logout");setUser(null)}}/>:<Auth onAuth={setUser}/>}
export default function RoutedApp(){return <Routes><Route path="*" element={<App/>}/></Routes>}